<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.aioman.meet.moi_meet_flutter_sdk">

    <application
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config"> 

        <activity
            android:name="org.aioman.meet.moi_meet_flutter_sdk.WrapperJitsiMeetActivity"
            android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
            android:launchMode="singleTask"
            android:theme="@style/JitsiMeetActivityStyle"
            android:resizeableActivity="true"
            android:showOnLockScreen="true"
            android:supportsPictureInPicture="true"
            android:windowSoftInputMode="adjustResize"
            android:exported="true"
            tools:replace="android:label">

            <!-- Intent filter for deep linking or external launch -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="meet.moi.org" />
            </intent-filter>
        </activity>

    </application>
</manifest>
